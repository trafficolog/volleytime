---
id: '9.10.2'
phase: '9'
epic: '9.10'
status: done
sync_state: synced
last_reviewed: 2026-09-16
status_note: ''
review_ref: 'Ревью v0.1.1 · Phase 9 · P1'
priority: P1
roles:
  - DEVOPS
depends_on: []
estimated_hours: '0.5'
tags:
  - ci
  - deploy
  - review2-fix
---

# Task 9.10.2: Откат деплоя поднимает действительно предыдущий релиз

## Цель

Шаг отката возвращает версию, которая работала до выкладки.

## Контекст

`.env.images` перезаписывался новыми тегами ДО копирования в `.env.images.previous` — «предыдущие» образы оказывались новыми, и откат перезапускал тот же релиз.

## Что должно быть сделано

1. Копировать `.env.images` → `.env.images.previous` ПЕРВОЙ командой, до генерации нового файла.
2. Шаг отката: если `.previous` совпадает с текущим — считать, что откатывать нечего, и падать с понятным сообщением.

## Критерии приёмки

- ✅ В скрипте копирование идёт до записи новых тегов
- ✅ При совпадении версий шаг отката сообщает об этом и завершается ошибкой

## Подсказки

-

## Не делать

-
