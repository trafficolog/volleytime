---
id: '5.13.21'
phase: '5'
epic: '5.13'
status: in_progress
sync_state: synced
last_reviewed: 2026-09-24
status_note: 'Локальные критерии и независимое ревью выполнены; карточка остаётся in_progress до PR, CI и merge в main. Реальный Telegram QA остаётся в 8.10.3.'
roles: [BACK, FE, QA]
depends_on: ['5.13.20', '3.11.2']
estimated_hours: '8-12'
tags: [subscriptions, organizations, settings, mvp, redesign]
---

# Task 5.13.21: Включение абонементов на уровне организации

## Цель

Дать владельцу возможность временно отключить новые продажи и использование абонементов в группе, сохранив купленные остатки и историю. [Согласованный дизайн](../superpowers/specs/2026-09-24-organization-subscriptions-toggle-design.md).

## Контекст

Сейчас у организации нет признака доступности абонементов: покупка и запись с них разрешены на любом платном событии. Простое скрытие интерфейса не защищает прямые API-запросы и может скрыть оплаченный остаток. В референсе v2 есть общий переключатель группы; отдельного переключателя события в MVP не будет.

## Что должно быть сделано

1. Добавить миграцию и поле `subscriptionsEnabled` у организации с `NOT NULL DEFAULT true`, расширить валидацию PATCH и аудит изменения; сохранить owner-only право на запись.
2. При `false` предметно отклонять сервером создание новых планов, покупку абонемента и новую запись с методом `subscription`. Не менять cash/transfer/free и существующие подтверждённые записи или очередь, оформленную до переключения.
3. Добавить переключатель и пояснение в текущие настройки группы. В текущем Mini App скрывать предложения покупки и способ оплаты абонементом. Пункт «Абонементы» оставлять только при наличии действующего остатка, показывая его без покупки; при отсутствии остатка скрывать. Прямая ссылка на собственные данные остаётся читаемой.
4. При повторном включении снова разрешить покупки и использование действующих неистёкших остатков без изменения их срока, количества и истории. Состояние брать из API текущей организации; ошибка загрузки не должна открывать запрещённые действия.
5. TDD: сервисные, API и UI-тесты на оба значения, owner-only право, остаток/без остатка, подтверждение ранее оформленной pending-покупки, прямой URL, смену группы, уже оформленные записи, очередь, отмену и повторное включение.

## Критерии приёмки

- После миграции существующие организации продолжают работать с абонементами; переключение сохраняется и аудируется. Владелец может менять настройку, другие роли не могут.
- При отключении новые покупка/план/запись с абонементом отвергаются в core/API независимо от UI; cash, transfer и free не ломаются.
- Купленные абонементы, остатки, pending и история не удаляются. Действующий остаток виден игроку без возможности новой покупки или выбора его при новой записи; без действующего остатка пункт меню скрыт. Прямая авторизованная ссылка остаётся доступной для истории.
- Ранее оформленные подтверждённые записи и очередь сохраняют условия; отмена корректно возвращает сессию. При повторном включении действующий неистёкший остаток снова доступен.
- Пять gates (`format:check`, `lint`, `typecheck`, `test`, `build`) зелёные. Визуальный smoke 320/390 px и Telegram QA фиксируются отдельно; tests не выдаются за production-проверку.

## Локальное свидетельство (2026-09-24)

- Схема, миграция `0019` (`DEFAULT true NOT NULL`), owner-only PATCH и аудит проверены интеграционными тестами. Новые план, покупка и запись по абонементу возвращают предметный `409`; cash/transfer/free и ранее оформленные обязательства сохраняются. После повторного включения действующий остаток снова разрешён.
- После исправления замечаний ревью `pnpm format:check`, `pnpm lint` (0 ошибок; предупреждения вне изменённых файлов), `pnpm typecheck` (6/6), `pnpm test` (86 файлов / 471 тест), `pnpm build` (2/2) выполнены в изолированной локальной ветке.
- Три сценария первого ревью воспроизведены RED→GREEN: устаревшая вкладка настроек не отправляет неизменённый флаг; запоздавший ответ другой организации или более ранней загрузки не заменяет текущие абонементы; параллельные переключения фиксируют фактическое предыдущее значение в аудите. Повторное независимое ревью диапазона `1fd5299..3376537` не установило Critical/Important дефектов и дало вердикт «готово к merge задачи после CI». Оно не запускало gates и не повторяло браузерный smoke самостоятельно; текущий локальный прогон подтверждён отдельно: 86 файлов / 471 тест, format, lint, typecheck и build зелёные.
- Неблокирующие рекомендации ревью: усилить сквозной тест последовательности pending-покупка → подтверждение при выключении → отказ записи → повторное включение; добавить конкурентный тест disable против новой записи/покупки; при появлении компонентного harness автоматизировать уже выполненные браузерные сценарии. Они не заменяют ручной Telegram QA.
- Локальный браузер с тестовой PostgreSQL: владелец выключил/включил опцию с проверкой после перезагрузки; при ошибке PATCH значение переключателя вернулось к подтверждённому состоянию. У игрока с остатком 8/8 при выключении история и пункт меню доступны только для чтения, покупка и оплата абонементом скрыты; у игрока без остатка пункт скрыт. Переключение на другую организацию не перенесло чужой остаток. Прямые URL истории и списка прежних планов оставались доступными; после включения действия появились снова. Проверены экраны 320/390 CSS px без горизонтального переполнения.
- Это не проверка в реальном Telegram-клиенте, включая его тёмную тему, и не production-проверка. Telegram-host QA остаётся в задаче 8.10.3; релиз R0.6 и `prod` не продвигались.

## Подсказки

Начать с `packages/db/src/schema/organizations.ts`, `packages/core/src/organizations`, `packages/core/src/subscriptions`, `packages/core/src/bookings`, существующих API и `apps/web/app/pages/m/orgs/[orgId]/settings.vue`. Дизайн 8.10.1 потребляет это правило, а 8.10.2 и 5.15.1 отображают его в новых экранах организатора.

## Не делать

- Не удалять и не обнулять уже купленные абонементы.
- Не добавлять per-event переключатель, multi-seat, split price, онлайн-оплату или автоматизацию будущих фаз.
- Не считать скрытие кнопки достаточной серверной защитой.
