---
id: '3.11.4'
phase: '3'
epic: '3.11'
status: todo
sync_state: synced
last_reviewed: 2026-09-25
status_note: 'Письменная спецификация принята пользователем; план реализации подготовлен и ожидает просмотра. Зависимость 3.11.2 ещё in_progress; код и QA не начаты.'
roles: [FE, QA]
depends_on: ['3.11.2']
estimated_hours: '12-18'
tags: [landing, mvp, redesign, accessibility]
---

# Task 3.11.4: Публичный MVP-лендинг v2

## Цель

Заменить минимальную главную `/` лендингом по актуальному `0 Лендинг.html`, сохранив Bento Bold и полный применимый интерактив, но описывая только действующие возможности MVP.

## Контекст

Пользователь включил лендинг в R0.6 отдельной задачей и подтвердил границу «только действующий MVP», а также частицы, параллакс, раскрытия и hover-эффекты при корректном `prefers-reduced-motion`. [Письменная спецификация](../superpowers/specs/2026-09-25-mvp-landing-v2-design.md) фиксирует состав, CTA и ограничения. Источник — архив `D:\ai\freelance\volleyball-volleytime\Volley Time v2.zip`, SHA-256 `51827733915F8D2B82F3CB4584AD26B50BCF722A3D2473AC18E9D12C7CC60657`: `0 Лендинг.html`, `landing-fx.js`, `styles.css` и сопоставление `01–03-landing.png` из `references/README.md`. Прототип — визуальная ссылка, не источник доменных правил или продакшен-код.

## Что должно быть сделано

1. Перестроить публичный маршрут `/` в существующем Nuxt-приложении: навигация, hero с демонстрационной иллюстрацией, problem/solution, Bento-карточки MVP-функций, три шага, блоки организатора/игрока, FAQ, финальный CTA и footer. Не переносить блок credits-тарифов; убрать его якорь и все несуществующие ссылки.
2. Привязать «Создать группу» к реальному email-входу с относительным `redirect` на `/m/orgs/create`; после определения действующей сессии вести сразу на создание. «Войти» ведёт на `/auth/login`, Telegram CTA показывается только при настроенном `telegramBotUsername` и ведёт к этому боту. Якоря ведут к существующим секциям.
3. Реализовать применимые эффекты макета средствами Vue/CSS/canvas с остановкой вне экрана/при скрытой вкладке и очисткой обработчиков. На touch нет курсорного параллакса/магнитных кнопок. При `prefers-reduced-motion` непрерывное движение, параллакс и счётчики отключены; контент остаётся видимым и FAQ/CTA работают без анимации и JavaScript-эффектов.
4. Использовать действующие токены/компоненты и русские тексты без неподтверждённых тарифов, подарочных событий, fake live-событий и обещаний функций будущих релизов. Декоративный пример интерфейса обозначить как пример, не выдавать за данные реальной организации.
5. Провести TDD для поведения ссылок/CTA и интерактивных состояний, visual/functional QA на 320/390/768/1280/1440 px, 200% zoom, с клавиатуры и reduced motion, затем пять обязательных gates.

## Критерии приёмки

- Каждая публичная ссылка и CTA имеют действующую цель; анонимный посетитель после входа достигает формы создания группы, пользователь с активной сессией идёт к ней напрямую. При отсутствии имени бота нет пустой Telegram-ссылки.
- Структура и ключевая композиция соответствует обновлённому лендингу, но видимый текст и примеры не обещают credits, split-оплату, multi-seat, сборы, отчёты, плановые напоминания или 30-минутное подтверждение очереди.
- FAQ, якоря и CTA работают с клавиатуры и без анимационного слоя; изображения и canvas не мешают чтению, есть видимый focus, один `h1`, `main` и логичный порядок заголовков.
- На малых ширинах и при 200% zoom нет горизонтального переполнения или недоступных действий; reduced motion отключает пространственное/непрерывное движение. Эффекты не продолжают потреблять кадры вне экрана и после ухода со страницы.
- SEO title/description отражают MVP. Пять локальных gates зелёные, browser smoke задокументирован; для релиза 8.10.3 выполняет сквозную проверку вместе с обновлённой авторизацией. Код задачи не продвигается в `prod` до общего R0.6 gate.

## Подсказки

- Текущая `/` уже имеет `telegramBotUsername` из runtime config. `/auth/login` поддерживает `?redirect=...`, а `/m/orgs/create` — существующая защищённая форма. Прямой переход на `/m/orgs/create` у анонимного пользователя сейчас теряет адрес при auth middleware, поэтому первичный публичный CTA должен вести через явный login redirect.
- Нативный `<details>/<summary>` подходит для FAQ; canvas и декоративный макет скрываются от accessibility tree, а смысловые подписи остаются обычным HTML.

## Не делать

- Не копировать React/Babel и `landing-fx.js` как продакшен-скрипт, не добавлять второй UI-kit или API ради статичного лендинга.
- Не публиковать тарифы credits, цены, «5 бесплатных событий», лимиты мест и сроки/автоматизацию, которых нет в текущем MVP.
- Не использовать фиктивные метрики, уведомления или имена как якобы живые данные; не делать фальшивые `href="#"` для поддержки, политики и примеров.
