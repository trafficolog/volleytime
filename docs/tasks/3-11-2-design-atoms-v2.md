---
id: '3.11.2'
phase: '3'
epic: '3.11'
status: done
sync_state: synced
last_reviewed: 2026-09-25
status_note: 'Код атомов из PR #38 и исправление индикатора активной вкладки из PR #42 объединены в main; TDD, пять gates, browser-проверка, независимое review и CI пройдены. Сквозная visual/Telegram QA остаётся в 8.10.3, production не проверялся.'
roles: [FE, QA]
depends_on: ['3.11.1']
estimated_hours: '6-9'
tags: [design-system, mvp, redesign]
---

# Task 3.11.2: Компоненты и chrome Bento Bold

## Цель

Перенести применимые к MVP компоненты Brand & System v2 в существующий Nuxt UI.

## Контекст

[Карта v2](../design/2026-09-23-reference-v2.md): поверхности без постоянной обводки, крупные числа, hero, состояния полей и тёмная плавающая tab bar.

## Что должно быть сделано

1. Обновить кнопки, карточки, чипы, поля/label, аватар, meter, empty/error/loading и Mini App header/tab bar существующей компонентной системы.
2. Сохранить семантику `<button>`/`<a>`, клавиатуру, focus-visible, disabled/loading, hit area 44 px в Mini App и reduced motion.
3. Написать проверки вариантов/состояний компонентов и smoke на навигацию.

## Критерии приёмки

- Каждый используемый MVP-вариант и интерактивное состояние сопоставлены с Brand & System v2.
- Навигация и формы работоспособны клавиатурой, фокус видим, цвета не единственный признак статуса.
- Пять обязательных gates зелёные.

## Свидетельства приёмки 2026-09-25

Дополнительное замечание: у `.vt-tabbar [data-active='true']` видимое состояние меняло только цвет. Атрибут `aria-current="page"` сообщал статус скринридеру, но не давал зрячему пользователю независимого от цвета признака. Исправление добавляет короткую линию под активной вкладкой; регрессионная проверка сначала упала на отсутствии индикатора, затем прошла после CSS-правки.

Исходник — очищенный `Volley Time v2.zip` SHA-256 `51827733915F8D2B82F3CB4584AD26B50BCF722A3D2473AC18E9D12C7CC60657`, в частности `02-brand.png`, `atoms.jsx` и `styles.css`. Сверка ограничена атомами действующего MVP; композиция экранов и будущие компоненты прототипа — отдельные задачи.

| Атом / состояние          | Сопоставление с референсом и реализацией                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Кнопки                    | `main.css`: `vt-btn` primary/ink/ghost, размеры sm/lg, disabled, focus-visible и reduced motion; `atoms.test.ts` проверяет контракт. В локальном браузере видны primary/ghost/ink на форме входа, пустом состоянии и экране группы.                                                                                                                                                            |
| Поверхности / карточки    | `vt-card` base/raised/hero/warm/cool из Brand & System; `atoms.test.ts` проверяет тональные поверхности без постоянной рамки. Скриншот группы показывает карточки и CTA; данные кассы тестовые, не эталон бизнес-состояния.                                                                                                                                                                    |
| Чипы, аватары, meter      | `VtChip.vue`, `VtAvatar.vue`, `VtMeter.vue` и селекторы `vt-chip`/`vt-avi`/`vt-meter` сопоставлены с применимыми вариантами `02-brand.png`; CSS-контракты проверены `atoms.test.ts`. Состояния на реальном событии остаются в сквозной матрице 8.10.3.                                                                                                                                         |
| Поля, empty/error/loading | `vt-field`, `EmptyState.vue`, `ErrorState.vue`, `SkeletonList.vue` используют существующие токены; тесты покрывают CSS и семантику загрузки. В браузере локальная форма показала 44 px высоты и после CSS-перехода клавиатурный `:focus-visible` с синей рамкой, 3 px кольцом и 2 px outline; empty state виден на новой группе.                                                               |
| Mini App chrome           | `MiniHeader.vue`, `TabBar.vue`, `miniapp-org.vue` сопоставлены с плавающей тёмной панелью референса. В браузере вкладки имеют 58 px высоты, `aria-current="page"` только у активной, Tab проходит их по порядку, Enter открывает `/m/orgs/1/events`; при 320 px `scrollWidth = innerWidth = 320`. Активная вкладка дополнительно имеет белую линию 16 × 2 px; у неактивной псевдоэлемента нет. |

Проверено на чистом `origin/main` `d1a0c9e`: `pnpm format:check` — pass; `pnpm lint` — exit 0, 0 errors и 22 существующих warnings; `pnpm exec turbo run typecheck --force` — 6/6 без cache replay; `pnpm test` — 82 files, 442/442; `pnpm build` — 2/2, веб-сборка выполнена без кеша. `pnpm test:unit` отдельно — 32 files, 138/138. Первый параллельный запуск format оборвался из-за lockfile contention pnpm (`EPERM`), одиночный повтор прошёл. В GitHub PR #38 смёржен 2026-09-24 как `78808a3`; три check runs на merge SHA — success: Test (unit + integration), Build, Lint · Format · Typecheck.

После исправления индикатора: `pnpm format:check` — pass после удаления только локальных генерированных Playwright YAML; `pnpm lint` — 0 errors, те же 22 warnings; `pnpm typecheck` — 6/6; `pnpm test` — 82 files, 443/443; `pnpm build` — 2/2. Отдельная проверка в Edge на фактических CSS-файлах при ширине 320 px: активная ссылка и соседняя ссылка имеют высоту 58 px, линия активной — 16 × 2 px, у неактивной `content: none`; скриншот `output/playwright/tabbar-indicator-320.png` не входит в коммит.

При смене `data-active` линия перешла на новую активную ссылку, прежняя потеряла её; `aria-current="page"` остался только у новой активной, горизонтальной прокрутки при 320 px нет. Независимое code review не выявило Critical/Important замечаний; минорное замечание об ослабленной проверке `content`/заливки исправлено в тесте, после чего повторно прошли все пять gates.

PR #42: CI на `3864c83` — Build, Lint · Format · Typecheck и Test (unit + integration) прошли. Эта карточка отмечается `done` в том же PR; статус действителен после его merge в `main`.

Browser QA: Edge/Playwright на локальном `127.0.0.1:3101` с тестовой PostgreSQL `volleytime_test`, console-email OTP и группой `QA atoms 2026-09-25` (`/m/orgs/1`). Проверены 390 px overview, 320 px events, клавиатурный вход/навигация и настройки. Режим `.vt-dark` проверен локально как предпросмотр токенов, **не** как реальный Telegram-host QA. При `prefers-reduced-motion: reduce` вычисленные `transition-duration: 0s`, `animation-name: none`. Скриншоты локальной проверки лежат в `output/playwright/atoms-qa-390.png`, `atoms-events-320.png`, `atoms-events-dark-390.png` (не входят в коммит).

Вывод ограничен задачей атомов: реальные Telegram-состояния, полный визуальный проход игрока/организатора, актуальная авторизация и production входят в 8.10.3 и профильные R0.6-задачи; их завершение здесь не утверждается.

## Подсказки

- Пользоваться существующими CSS-атомами и `app/components/vt`; не подключать UI-kit.

## Не делать

- Не переносить React-компоненты буквально и не трогать доменную логику.
