---
id: '5.13.17'
phase: '5'
epic: '5.13'
status: done
sync_state: synced
last_reviewed: 2026-09-16
status_note: ''
review_ref: 'Phase 5 · Визуал «Список событий»'
priority: P2
roles:
  - BACK
  - FE
depends_on:
  - '5.13.11'
  - '5.13.16'
estimated_hours: '2'
tags:
  - events
  - ui
  - review-fix
---

# Task 5.13.17: Список событий: заполненность, «вы записаны», «мест нет», «+ Создать» только организатору

## Цель

Игрок видит в списке всё, чтобы решить, куда записаться.

## Контекст

Нет заполненности (9/14), отметки брони, «мест нет»; у заполненного «до 2 мест»; «+ Создать» виден всем и ведёт на `events/create`, который совпадает с `[eventId]`.

## Что должно быть сделано

1. `eventService.list` отдаёт `taken`, `waitlist`, `myBooking: { id, status } | null`, `venue: { name }`.
2. UI: карточка события — день/время, площадка, `VtMeter` taken/capacity, чипы «Вы записаны» / «В листе ожидания» / «Мест нет», цена.
3. «+ Создать» — только управляющим, ссылка на `/events/new` (5.13.19).

## Критерии приёмки

- ✅ Заполненность и статус брони видны без открытия события

## Подсказки

- Агрегаты одним запросом (GROUP BY event_id).

## Не делать

- ❌ Не делать N+1 запросов
